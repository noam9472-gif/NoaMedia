<Window x:Class="NoamediaLogin.MainWindow"        
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"       
        Title="Noamedia Login" Height="600" Width="900"         
        Background="Black" WindowStartupLocation="CenterScreen">
    <Grid>
        
        <!-- תיבת הכניסה המרכזית -->
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="252,351,0,0">
            <!--  שם משתמש -->
            <Border Background="#1A1A1A" BorderBrush="White" BorderThickness="2"  CornerRadius="5" 
                    Padding="15" Margin="0,0,0,20" Width="350" Height="52">
                <StackPanel>
                    <TextBlock Text="Enter user name" Foreground="White" FontSize="14" Margin="0,0,0,8"/>
                    <TextBox x:Name="UsernameTextBox" Background="Transparent"  Foreground="White" 
                             BorderThickness="0" FontSize="16"  CaretBrush="White"/>
                </StackPanel>
            </Border>
            <!--  סיסמה -->
            <Border Background="#1A1A1A" BorderBrush="White" BorderThickness="2" CornerRadius="5"
                    Padding="15" Margin="0,0,0,30" Width="350" Height="58">
                <StackPanel>
                    <TextBlock Text="Enter a password" Foreground="White" FontSize="14" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PasswordBox" Background="Transparent"                                   Foreground="White" BorderThickness="0" FontSize="16"  CaretBrush="White"/>
                </StackPanel>
            </Border>
            <!--  מעבר למשתמש רשום -->
            <Button x:Name="LoginButton" Content="Sign in" Click="LoginButton_Click" Width="350" 
                    Height="45" FontSize="18" FontWeight="Bold" Background="#E50914" Foreground="White" 
                    BorderThickness="0"  Cursor="Hand">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="#E50914"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}"                                              CornerRadius="5" Padding="10">
                                        <ContentPresenter HorizontalAlignment="Center"                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F40612"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <!--  מעבר לרישום -->
            <Button Content="Don't have an account? Sign up"  Background="Black"      
              BorderThickness="0" Foreground="LightGray" Cursor="Hand"        
              Click="SignupButton_Click" />
        </StackPanel>
        <!--לוגו-->
        <Image Margin="132,0,132,238" Source="/Image1.png" />

    </Grid>
</Window>     