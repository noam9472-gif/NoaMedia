<Page x:Class="NoaMedia.Pages.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:NoaMedia" 
      Title="Home" Background="#141414">

    <Page.Resources>
        <DataTemplate x:Key="MovieTemplate">
            <Grid Margin="0,0,15,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>



               
                    <Border Width="220" Height="125" CornerRadius="8" ClipToBounds="True" Background="#222">
                    <Image Source="{Binding MovieImageSource}" Stretch="UniformToFill"/>
                </Border>

                <Button Grid.Row="1" 
                        Content="Watch Now" 
                        Margin="0,8,0,0" 
                        Height="35" 
                        Background="#E50914" 
                        Foreground="White" 
                        FontWeight="Bold"
                        BorderThickness="0" 
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="30,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">

                <TextBlock Text="Hi " Foreground="White" FontSize="30" FontWeight="SemiBold"/>
                <TextBlock x:Name="UserNameText" Text="User" Foreground="#E50914" FontSize="30" FontWeight="Bold"/>
                <TextBlock Text="!" Foreground="White" FontSize="30" FontWeight="SemiBold"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="+ Add Movie" Click="AddMovie_Click" Width="110" Height="35" Background="#333" Foreground="White" BorderThickness="0" Margin="0,0,15,0" Cursor="Hand"/>
                <Button Click="Profile_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <Border Width="40" Height="40" Background="#E50914" CornerRadius="5">
                        <TextBlock Text="U" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                    </Border>
                </Button>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30,0,30,20">

                <TextBlock Text="Action" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,20,0,10"/>

                <Image x:Name="imgAction"/>


                <TextBlock Text="Romance" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="RomanceMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Horror" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <StackPanel x:Name ="baseSPhorror" Orientation="Vertical"/>
                
                <TextBlock Text="Comedy" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="ComedyMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Drama" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="DramaMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Fiction" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="SciFiMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Fantasy" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="FantasyMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Thriller" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="ThrillerMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Animation" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="AnimationMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Documentary" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="DocumentaryMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Crime" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="CrimeMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Mystery" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="MysteryMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />

                <TextBlock Text="Adventure" Foreground="White" FontSize="22" FontWeight="Bold" Margin="0,30,0,10"/>
                <ItemsControl x:Name="AdventureMoviesItems" ItemTemplate="{StaticResource MovieTemplate}" />



                

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>