<Page x:Class="NoaMedia.Pages.LogIn"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NoaMedia.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LogIn" Background="Black">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

        <Image Source="/pictures/Main Picture.png" Width="230" Margin="0,0,0,20"/>

        <Border Background="#1A1A1A" BorderBrush="White" Padding="10" BorderThickness="2" Width="350" Margin="0,5">
            <StackPanel>
                <TextBlock Text="Enter user name" Foreground="White" FontSize="14"/>
                <TextBox x:Name="UsernameTextBox" Background="Transparent" Foreground="White" 
                         BorderThickness="0" FontSize="16" CaretBrush="White"/>
            </StackPanel>
        </Border>

        <Border Background="#1A1A1A" BorderBrush="White" BorderThickness="2" Padding="10" Width="350" Margin="0,5">
            <StackPanel>
                <TextBlock Text="Enter a password" Foreground="White" FontSize="14" Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordBox" Background="Transparent" Foreground="White" 
                             BorderThickness="0" FontSize="16" CaretBrush="White"/>
            </StackPanel>
        </Border>

        <Button x:Name="LoginButton" Content="Sign in" Click="LoginButton_Click" Width="350" 
                Height="45" FontSize="18" FontWeight="Bold" Foreground="White" 
                Margin="0,15,0,0" Cursor="Hand">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Background" Value="#E50914"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#F40612"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="Don't have an account? Sign up" Background="Transparent"      
                BorderThickness="0" Foreground="LightGray" Cursor="Hand"        
                Click="SignupButton_Click" Margin="0,20,0,0"/>

    </StackPanel>
</Page>